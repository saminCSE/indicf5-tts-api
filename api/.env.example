PORT=3000
MONGO_URI=mongodb://localhost:27017/indicf5-tts
REDIS_HOST=localhost
REDIS_PORT=6380

# mock | real — mock generates silent WAVs without the Python model server
TTS_BACKEND=mock
TTS_MAX_CHARS=1000
QUEUE_MAX_DEPTH=100
JOB_TIMEOUT_MS=120000
# only used when TTS_BACKEND=real
TTS_SERVICE_URL=http://localhost:8000
